{
  "name": "nbody-wars",
  "version": "1.0.0",
  "description": "Asteroids-like game with N-body gravitational physics",
  "type": "module",
  "scripts": {
    "build:wasm": "cd engine && emmake make",
    "build:ts": "vite build",
    "build": "npm run build:wasm && npm run build:ts",
    "dev": "npm run build:wasm && vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.8"
  }
}
